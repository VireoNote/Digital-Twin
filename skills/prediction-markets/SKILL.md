---
name: prediction-markets
description: 交易架构2.0 - 预测市场共识提取器。调用 Polymarket 预测市场数据，扫描突发政策与宏观事件的叙事动量。用于在贝叶斯模型中提供叙事溢价补偿 (Narrative Premium) 并动态修正宏观底色。
---

# 预测市场共识提取器 (Doppler Radar Agent)

本技能属于“交易架构 2.0”的特征提取模块 之一。
核心职责是监控真金白银计价的预测市场，弥补传统宏观/微观量化模型对于“未来预期”和“突发事件”的盲区。

## 核心算法与法则
本专员遵循 L3 系统约束中的绝对算法：
1. **硬性物理过滤**：只抓取大资金（>100万）、强动量、近端（<30天）的事件，忽略长尾死水。
2. **贝叶斯偏移 ($\Delta E_{narrative}$)**：输出的不是绝对利多或利空信号，而是作为环境系数的“偏置项”。

## 工作流与执行指令 (Execution)

当用户请求分析市场预期、突发事件影响或执行 v2.0 完整预报时，执行以下步骤：

1. **执行抓取脚本**：
   运行捆绑的 Python 脚本，以拉取最新的 Polymarket 高动量事件。
   ```bash
   python3 scripts/fetch_prediction_markets.py
   ```
2. **分析结果**：
   该脚本会将过滤后的关键预期事件存入 `L1 原始事件层`（`./digital_twin/Inbox/`）。读取最新生成的雷达报告。
3. **输出交叉验证报告**：
   提取当前的高能叙事事件。**作为系统主进程，你必须主观判断这些事件的冲击方向（I 值正负）**，并在最终预报中说明它们是如何修正原有宏观底色 $E_{macro}$，从而影响最终微观阈值的。
